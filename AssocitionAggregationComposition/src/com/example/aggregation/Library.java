package com.example.aggregation;
import java.util.*;

public class Library {

	private String name;
	
	private List<Book> bookLists;
	
	/*
	 * Aggregation means one object owns , another object.
	 * If we delete the parent object, still child object or the another
	 * object will exist independently.
	 * It represent weak relationship
	 */
}
